version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout
     - run: docker build -t orbs/orbs-network:$CIRCLE_BRANCH .
     - run: docker run -ti orbs/orbs-network:$CIRCLE_BRANCH bash -x test.sh
