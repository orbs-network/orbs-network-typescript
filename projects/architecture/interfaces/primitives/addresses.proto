syntax = "proto3";

message UniversalAddress {
  uint32 scheme = 1;
  uint32 networkId = 2;
  bytes id = 3; // TODO: is id the right name? maybe key?
  uint32 checksum = 4;
}

message ContractAddress {
  string address = 1; // TODO: what's the format?
}
